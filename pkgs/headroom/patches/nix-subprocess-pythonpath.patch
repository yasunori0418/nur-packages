Binary files a/headroom/cli/__pycache__/wrap.cpython-314.pyc and b/headroom/cli/__pycache__/wrap.cpython-314.pyc differ
diff -ruN a/headroom/cli/wrap.py b/headroom/cli/wrap.py
--- a/headroom/cli/wrap.py	2026-06-18 23:49:06.239768307 +0900
+++ b/headroom/cli/wrap.py	2026-06-18 23:49:24.512215954 +0900
@@ -329,7 +329,7 @@
     deadlocks (macOS pipe buffer is ~64KB — a busy proxy fills it quickly,
     blocking the process).
     """
-    cmd = [sys.executable, "-m", "headroom.cli", "proxy", "--port", str(port)]
+    cmd = (([shutil.which("headroom")] if shutil.which("headroom") else [sys.executable, "-m", "headroom.cli"]) + ["proxy", "--port", str(port)])
 
     # Forward HEADROOM_MODE env var so the proxy respects the user's mode choice
     headroom_mode = os.environ.get("HEADROOM_MODE")
@@ -1513,12 +1513,14 @@
     # Use forward slashes in TOML paths (works on all platforms, avoids
     # backslash escaping issues on Windows)
     python_bin = sys.executable.replace("\\", "/")
+    mcp_pythonpath = os.pathsep.join(p for p in sys.path if p).replace("\\", "/")
     db_path_toml = db_path.replace("\\", "/")
     mcp_section = (
         f"\n{_MEMORY_MCP_MARKER}\n"
         f"[mcp_servers.headroom_memory]\n"
         f'command = "{python_bin}"\n'
         f'args = ["-m", "headroom.memory.mcp_server", "--db", "{db_path_toml}", "--user", "{user_id}"]\n'
+        f'env = {{ PYTHONPATH = "{mcp_pythonpath}" }}\n'
         f"startup_timeout_sec = 30\n"
         f"tool_timeout_sec = 30\n"
         f"{_MEMORY_MCP_END}\n"
@@ -2897,6 +2899,7 @@
                     "claude",
                     "--force",
                 ],
+                env={**os.environ, "PYTHONPATH": os.pathsep.join(filter(None, [*sys.path, os.environ.get("PYTHONPATH", "")]))},
                 capture_output=True,
                 text=True,
                 timeout=30,
Binary files a/headroom/evals/__pycache__/comprehensive_benchmark.cpython-314.pyc and b/headroom/evals/__pycache__/comprehensive_benchmark.cpython-314.pyc differ
Binary files a/headroom/evals/__pycache__/suite_runner.cpython-314.pyc and b/headroom/evals/__pycache__/suite_runner.cpython-314.pyc differ
diff -ruN a/headroom/evals/comprehensive_benchmark.py b/headroom/evals/comprehensive_benchmark.py
--- a/headroom/evals/comprehensive_benchmark.py	2026-06-18 23:49:06.303223424 +0900
+++ b/headroom/evals/comprehensive_benchmark.py	2026-06-18 23:49:52.074964830 +0900
@@ -269,6 +269,7 @@
     start_time = time.time()
     env = {
         **os.environ,
+        "PYTHONPATH": os.pathsep.join(filter(None, [*sys.path, os.environ.get("PYTHONPATH", "")])),
         "TOKENIZERS_PARALLELISM": "false",
         "HF_ALLOW_CODE_EVAL": "1",  # Required for humaneval/mbpp tasks
     }
diff -ruN a/headroom/evals/suite_runner.py b/headroom/evals/suite_runner.py
--- a/headroom/evals/suite_runner.py	2026-06-18 23:49:06.288020816 +0900
+++ b/headroom/evals/suite_runner.py	2026-06-18 23:49:24.512594331 +0900
@@ -277,6 +277,7 @@
     try:
         proc = subprocess.Popen(
             [sys.executable, "-m", "headroom.proxy.server", "--port", str(port)],
+            env={**os.environ, "PYTHONPATH": os.pathsep.join(filter(None, [*sys.path, os.environ.get("PYTHONPATH", "")]))},
             stdout=subprocess.PIPE,
             stderr=subprocess.PIPE,
         )
Binary files a/headroom/install/__pycache__/runtime.cpython-314.pyc and b/headroom/install/__pycache__/runtime.cpython-314.pyc differ
diff -ruN a/headroom/install/runtime.py b/headroom/install/runtime.py
--- a/headroom/install/runtime.py	2026-06-18 23:49:06.256312841 +0900
+++ b/headroom/install/runtime.py	2026-06-18 23:49:24.511414448 +0900
@@ -88,7 +88,7 @@
     """Build the raw foreground command that runs the proxy."""
 
     if manifest.runtime_kind == RuntimeKind.PYTHON.value:
-        return [sys.executable, "-m", "headroom.cli", "proxy", *manifest.proxy_args]
+        return [*resolve_headroom_command(), "proxy", *manifest.proxy_args]
 
     _ensure_host_dirs()
     home = str(Path.home())
Binary files a/headroom/memory/adapters/__pycache__/hnsw.cpython-314.pyc and b/headroom/memory/adapters/__pycache__/hnsw.cpython-314.pyc differ
diff -ruN a/headroom/memory/adapters/hnsw.py b/headroom/memory/adapters/hnsw.py
--- a/headroom/memory/adapters/hnsw.py	2026-06-18 23:49:06.272207912 +0900
+++ b/headroom/memory/adapters/hnsw.py	2026-06-18 23:49:24.512465831 +0900
@@ -57,6 +57,7 @@
 
     # Use subprocess to safely probe for hnswlib - if it crashes with SIGILL,
     # only the subprocess dies, not our main process
+    import os
     import subprocess
     import sys
 
@@ -66,6 +67,7 @@
         probe_code = "import hnswlib; hnswlib.Index(space='cosine', dim=4)"
         result = subprocess.run(
             [sys.executable, "-c", probe_code],
+            env={**os.environ, "PYTHONPATH": os.pathsep.join(filter(None, [*sys.path, os.environ.get("PYTHONPATH", "")]))},
             capture_output=True,
             timeout=10,
         )
